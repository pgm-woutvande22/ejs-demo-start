<nav class="primary">
    <% if(navItems) {%>
        
            <% navItems.forEach(item=> { %>
                
                    <a href="<%=item.url %>"" ><%= item.label %></a>
                    
                    <% }); %>
            
            <% } %>
    </nav>